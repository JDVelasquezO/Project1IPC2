@model Entity.Contact_Entity

@{
    ViewBag.Title = "HomeContact";
    Layout = "~/Views/Layouts/_LayoutContact.cshtml";

    int idContact = Convert.ToInt32(Session["adminContact"]);
}

<main class="container">
@{ 
    Html.RenderAction("SearchComercialContact", "Contact", new { id = idContact });
}
</main>